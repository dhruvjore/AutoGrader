# This will hold your grading logic later

def evaluate_assignment(student_text: str, rubric_text: str) -> dict:
    """
    Dummy evaluator function. Later we’ll connect to LLM (Prometheus-2 7B) + RAG.
    """
    return {
        "grade": "B+",
        "feedback": "Good attempt. Improve structure and add more references."
    }
